# 资源_客户端与机器人

## `kook库.资源.客户端` —— Bot 聚合客户端

KOOK 机器人聚合客户端模块：以 Bot 令牌鉴权，按域装配全部可用分类资源供业务直接调用。

## `HTTP客户端`

机器人 HTTP 客户端：以 Bot 令牌鉴权，聚合机器人可用的全部分类资源。

> 源码位置：`\资源\客户端.py` 第 24 行

- **解析频道ID** — 把频道 id 或频道名称解析为频道 id(名称查询带缓存)。

  ```python
  def 解析频道ID(self, 服务器ID: str, 频道标识: str) -> str
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 纯数字 id 直接返回；名称匹配以服务器频道列表中第一个同名频道为准。
> 参数:
> 服务器ID: 频道所在服务器 id。
> 频道标识: 频道 id 或频道名称。
> 返回:
> 频道 id。
> 异常:
> ValueError: 服务器下未找到该名称的频道。

</details>

- **解析服务器ID** — 把服务器 id 或服务器名称解析为服务器 id(名称查询带缓存)。

  ```python
  def 解析服务器ID(self, 服务器标识: str) -> str
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 服务器标识: 服务器 id 或服务器名称。
> 返回:
> 服务器 id。
> 异常:
> ValueError: 未找到该名称的服务器。

</details>

- **获取网关地址** — 获取 WebSocket 网关连接地址。

  ```python
  def 获取网关地址(self) -> str
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 返回:
> 网关 URL，compress=0 保证数据明文文本而非 zlib 压缩。

</details>

## `kook库.资源.机器人` —— 机器人主类(含语法糖)

KOOK 机器人主模块：组合 REST 聚合客户端与 WebSocket 网关，提供事件注册/分发、启停管理，

### `推断媒体类型`

按文件名扩展名推断应使用的消息类型(语法糖内部工具)。

```python
def 推断媒体类型(名称: str | None) -> int
```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 名称: 文件名或资源 url。
> 返回:
> 消息类型：图片2 / 视频3 / 其它一律按文件4。

</details>

## `机器人`

KOOK 机器人主类。 启动后同时提供 REST 调用(self.API)与实时事件分发；处理器通过装饰器注册。 事件处理器可直接使用 事件.回复/事件.引用回复/事件.私信作者 便捷发送。

> 源码位置：`\资源\机器人.py` 第 44 行

- **注册处理器** — 注册一个事件处理器(装饰器用法)。

  ```python
  def 注册处理器(self, 事件类型: int | str | None = None)
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 事件类型: 普通消息类型编号(int，见消息类型枚举)；
> 系统事件名(str，见系统事件名枚举)；
> None 表示匹配所有事件。
> 返回:
> 装饰器，用于包裹异步处理函数，处理器接收一个 事件 对象参数。

</details>

- **文本消息** — 便捷注册：监听频道/私信的文字消息(类型 1 纯文本与 9 KMD 文本)。返回: 装饰器。

  ```python
  def 文本消息(self)
  ```

- **命令** — 便捷注册：命令路由(仅文本消息)，处理器接收 (事件, 命令参数)。

  ```python
  def 命令(self, 命令文本: str)
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 触发条件为消息文本去掉首尾空白后以 命令文本 开头。
> 参数:
> 命令文本: 命令前缀，如 "!ping"。
> 返回:
> 装饰器，处理器签名 async def 处理(事件: 事件, 参数: str)。

</details>

- **关键词** — 便捷注册：包含关键词的文本消息路由，处理器接收 (事件)。

  ```python
  def 关键词(self, 关键词文本: str)
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 关键词文本: 消息正文中需包含的文本。
> 返回:
> 装饰器，处理器签名 async def 处理(事件: 事件)。

</details>

- **按钮点击** — 便捷注册：监听卡片按钮点击系统事件(message_btn_click)。返回: 装饰器。

  ```python
  def 按钮点击(self)
  ```

- **启动时** — 注册连接成功后的启动钩子。返回: 原处理函数。

  ```python
  def 启动时(self, 处理器: Callable[[ForwardRef('机器人')], Awaitable[NoneType]]) -> Callable[[ForwardRef('机器人')], Awaitable[NoneType]]
  ```

- **关闭时** — 注册关闭前的清理钩子。返回: 原处理函数。

  ```python
  def 关闭时(self, 处理器: Callable[[ForwardRef('机器人')], Awaitable[NoneType]]) -> Callable[[ForwardRef('机器人')], Awaitable[NoneType]]
  ```

- **发送频道** — 便捷：向频道发送文本或卡片消息(= API.频道消息.发送)。

  ```python
  def 发送频道(self, 频道ID: str, 内容: str | list | dict, 类型: int | None = None, 引用消息ID: str | None = None) -> Any
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 频道ID: 目标频道 id。
> 内容: 文本，或卡片数组/字典(自动按卡片类型发送)。
> 类型: 消息类型，默认自动推断。
> 引用消息ID: 引用回复的源消息 id。
> 返回:
> 发送接口的 data(含 msg_id)。

</details>

- **发送私信** — 便捷：向用户发送私信(= API.私信消息.发送)。

  ```python
  def 发送私信(self, 用户ID: str, 内容: str | list | dict, 类型: int | None = None) -> Any
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 用户ID: 目标用户 id。
> 内容: 文本或卡片结构。
> 类型: 消息类型，默认自动推断。
> 返回:
> 发送接口的 data(含 msg_id)。

</details>

- **_媒体地址** — 把媒体来源统一转为可发送的资源 url(便捷媒体发送的内部工具)。

  ```python
  def _媒体地址(self, 来源: str | bytes, 文件名: str | None) -> str
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 来源: http(s) 开头的站内资源 url，本地文件路径，或文件字节。
> 文件名: 字节来源必需，用于扩展名与上传展示；路径来源可空。
> 返回:
> 资源 url；本地来源会先上传到素材库。
> 异常:
> ValueError: 字节来源未提供文件名或本地路径无效。

</details>

- **发送媒体** — 便捷：向频道发送本地文件/字节媒体，自动上传并按类型发送(= 素材上传 + 频道消息)。

  ```python
  def 发送媒体(self, 频道ID: str, 来源: str | bytes, 文件名: str | None = None, 类型: int | None = None) -> Any
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 频道ID: 目标频道 id。
> 来源: 本地文件路径、文件字节，或已上传的 http(s) 资源 url。
> 文件名: 字节来源必需(带扩展名)；可空时按来源推断。
> 类型: 消息类型(图片2/视频3/文件4)，None 按扩展名自动推断。
> 返回:
> 发送接口的 data(含 msg_id)。

</details>

- **私信媒体** — 便捷：向用户私信发送本地文件/字节媒体(= 素材上传 + 私信消息)。

  ```python
  def 私信媒体(self, 用户ID: str, 来源: str | bytes, 文件名: str | None = None, 类型: int | None = None) -> Any
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 用户ID: 目标用户 id。
> 来源: 本地文件路径、文件字节，或已上传的 http(s) 资源 url。
> 文件名: 字节来源必需(带扩展名)。
> 类型: 消息类型(图片2/视频3/文件4)，None 按扩展名自动推断。
> 返回:
> 发送接口的 data(含 msg_id)。

</details>

- **_事件发送器** — 事件便捷发送的内部实现(由 事件.回复/引用回复/私信作者 调用)。

  ```python
  def _事件发送器(self, 事件对象: 事件, 内容: str | list | dict, 类型: int | None, 引用: bool, 私信作者: bool) -> Any
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 按事件来源通道自动路由：频道消息回频道、私信回对方、指定则私信作者。
> 参数:
> 事件对象: 触发发送的原始事件。
> 内容: 文本或卡片结构。
> 类型: 消息类型。
> 引用: 是否带引用原消息。
> 私信作者: 是否改为私信给作者。
> 返回:
> 发送接口的 data(含 msg_id)。
> 异常:
> RuntimeError: 系统事件或未知通道类型无法定位回复目标。

</details>

- **等待事件** — 等待下一个满足条件的事件，适合做一问一答式交互。

  ```python
  def 等待事件(self, 谓词: Callable[[事件], bool], 超时秒: float = 60) -> 事件 | None
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 谓词: 判断函数，接收 事件 返回是否命中。
> 超时秒: 最长等待秒数，默认 60。
> 返回:
> 命中事件；超时返回 None。

</details>

- **等待文本** — 等待下一条符合过滤条件的文本消息。

  ```python
  def 等待文本(self, 关键词: str | None = None, 频道ID: str | None = None, 作者ID: str | None = None, 超时秒: float = 60) -> 事件 | None
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 关键词: 仅接收正文含该关键词的消息，None 不限制。
> 频道ID: 仅接收该频道的消息，None 不限制。
> 作者ID: 仅接收该作者的消息，None 不限制。
> 超时秒: 最长等待秒数，默认 60。
> 返回:
> 命中的文本消息事件；超时返回 None。

</details>

- **等待按钮点击** — 等待某按钮(value 匹配)被点击的系统事件。

  ```python
  def 等待按钮点击(self, 值: str, 超时秒: float = 60) -> 事件 | None
  ```

<details><summary>参数 / 返回 / 异常 完整说明</summary>

> 参数:
> 值: 目标按钮回传的 value。
> 超时秒: 最长等待秒数，默认 60。
> 返回:
> 按钮点击系统事件(主体含 value/user_id 等)；超时返回 None。

</details>

- **_处理网关事件** — 网关回调入口：包装事件、唤醒等待器、注入发送器并分发到匹配的处理器。

  ```python
  def _处理网关事件(self, 事件数据: dict[str, typing.Any]) -> None
  ```

- **打开** — 打开 HTTP 会话、启动网关并触发启动钩子。返回: 无返回值。

  ```python
  def 打开(self) -> None
  ```

- **关闭** — 触发关闭钩子，停止网关并关闭 HTTP 会话。返回: 无返回值。

  ```python
  def 关闭(self) -> None
  ```

- **_启动并等待** — 内部运行流程：打开→等待停止信号→关闭。返回: 无返回值。

  ```python
  def _启动并等待(self) -> None
  ```

- **运行** — 以阻塞方式运行机器人，直到收到停止信号或进程退出。返回: 无返回值。

  ```python
  def 运行(self) -> None
  ```

- **请求停止** — 请求机器人优雅停止(供运行在其他事件循环中的调用方使用)。返回: 无返回值。

  ```python
  def 请求停止(self) -> None
  ```
